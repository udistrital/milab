FROM node:25-slim

WORKDIR /usr/src/app

COPY package*.json ./
RUN npm ci --omit=dev

# Imagen para entorno local/pruebas: incluye utilidades usadas en validaciones.
COPY src ./src
COPY scripts ./scripts
COPY sql-scripts ./sql-scripts

EXPOSE 3000

CMD ["node", "src/app.js"]
